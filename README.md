# Telegram Bot для определения присутствия в Sprut.Hub

Позволяет отслеживать, кто из членов семьи дома, а кто ушёл, с помощью iPhone, Sprut.Hub и Telegram! Например, когда ты приходишь домой, бот напишет в чат "Вася дома", а когда уходишь — "Вася не дома". Всё работает через твой iPhone (по геопозиции) и Sprut.Hub.
## Что нужно
- iPhone с установленным приложением "Команды" (Shortcuts).
- Любой телефон с Telegram.
- Sprut.Hub.
- Руки, что бы копипастить и желание всё настроить. 😊

---

## Настройка (шаг за шагом)

### 1. Создаём ботов, канал и группу в Telegram
Нам нужно два бота, канал и группа, чтобы всё заработало. Канал будет получать команды от твоего телефона, а группа — показывать сообщения и служить для обсуждения.

#### Создаём первого бота (основной)
1. Открой Telegram и найди @BotFather (это бот, который создаёт других ботов).
2. Напиши ему `/newbot`.
3. Придумай имя для бота, например, "MyFamilyBot", и username — он должен заканчиваться на Bot, например, @MyFamilyBot.
4. @BotFather даст тебе токен (длинный код, например, 7777777777:AAAAAAAA7-aa7aa77AaAAaA7AaAAaA77aAA). Скопируй его или запомни.
5. Он понадобится в глобальном сценарии нужно будет найти строку `let token = ***твой_токен***;` и вставить туда этот токен (замени: ***твой_токен*** на свой токен бота).

#### Создаём второго бота (прокси)
1. Повтори шаги с @BotFather, но создай второго бота, например, "ProxyFamilyBot" (@ProxyFamilyBot).
2. Скопируй его токен — он понадобится позже для настройки автоматизации на iPhone.

#### Создаём канал
1. В Telegram нажми на три полоски вверху слева → "Создать канал".
2. Назови канал, например, "FamilyProxy", и сделай его **приватным** (чтобы никто лишний не зашёл).
3. Добавь обоих ботов (@MyFamilyBot и @ProxyFamilyBot) как администраторов:
   - Нажми на название канала → "Администраторы" → "Добавить администратора".
   - Найди бота и дай ему права отправлять сообщения.

#### Создаём группу
1. В Telegram нажми на три полоски вверху слева → "Создать группу".
2. Назови группу, например, "Семья", и добавь туда обоих ботов (@MyFamilyBot и @ProxyFamilyBot).
3. Связываем группу с каналом:
   - В настройках канала нажми "Обсуждение" → выбери группу "Семья".
   - Теперь все сообщения из канала будут автоматически дублироваться в группу, а группа станет местом, где бот будет писать "дома" или "не дома".

#### Находим ID канала и группы
1. Добавь в канал и группу бота @RawDataBot.
2. Напиши что-нибудь в канале и группе (например, "Привет").
3. @RawDataBot пришлёт тебе сообщение, где будет `chat_id` (например, `-1009999999999` для канала и `-1008888888888` для группы).
4. ID группы вставь в сценарий Sprut.Hub:
   - Найди строку `let chatIDs = [***твой_ид_группы***];`.
   - Замени `[***твой_ид_группы***]` на свой ID группы).
5. ID канала сохрани — он понадобится для настройки автоматизации на iPhone.

---

### 2. Создаём виртуальные устройства в Sprut.Hub
Для каждого члена семьи (например, Вася, Лена, Галя, Толя) нужно создать виртуальное устройство с двумя частями: выключателем и датчиком присутствия.

1. Открой Sprut.Hub.
2. Перейди в раздел "Устройства".
3. Нажми "+" → "Добавить виртуальное устройство".
4. Назови устройство (например, "Вася").
5. Добавь два сервиса:
   - **Виртуальный выключатель**:
     - Тип: "Switch".
     - Запиши его `accessoryId` (например, `101`).
   - **Виртуальный датчик присутствия**:
     - Тип: "Occupancy Sensor".
     - Запиши его `accessoryId` (например, `102`).
6. Повтори для каждого члена семьи (Лена, Галя, Толя и т.д.).

#### Связываем выключатель и датчик
Чтобы выключатель и датчик работали вместе (например, когда выключатель включён, датчик показывает "Есть присутствие"):
1. В Sprut.Hub зайди в настройки датчика присутствия (например, "Вася").
2. Выбери "Связь с устройством".
3. Нажми на "Обнаружение присутствия" → "Добавить".
4. Выбери выключатель этого же человека ("Вася", сервис "Switch").
5. Выбери тип синхронизации: "Последнее значение".
6. Сохрани настройки.
7. Теперь, если ты включишь выключатель вручную, датчик автоматически покажет "Есть присутствие", а если выключишь — "Нет присутствия".
8. Повтори для каждого члена семьи.
9. Скрываем выключатель (что бы не засорял экран).

---

### 3. Настраиваем сценарий в Sprut.Hub
Теперь нужно добавить сценарий, который будет обрабатывать команды из Telegram.

1. Открой Sprut.Hub и перейди в раздел "Сценарии".
2. Глобальные:
   - Нажми "+" → выбери "Импортировать шаблон".
   - Выбери скачанный файл сценария.
   - Назови его, например, "TelegramPresenceBot".
3. Обнови настройки в сценарии:
   - В строке `let chatIDs = [***твой_ид_группы***];` вставь ID своей группы (который ты узнала через @RawDataBot).
   - В строке `let token = ***твой_токен***;` вставь токен своего основного бота (@MyFamilyBot).
   - Найди секцию `const switchMap = { ... };` и добавь имена и ID аксессуаров для всех членов семьи:
     
     const switchMap = {
         'Вася': { accessoryId: 101 },
         'Лена': { accessoryId: 102 },
         'Галя': { accessoryId: 103 },
         'Толя': { accessoryId: 104 }
     };
     ```
     - Замени имена и `accessoryId` на свои. Например, для нового человека (Петя) добавь строку: 'Петя': { accessoryId: 105 }.
4. Сохрани сценарий.

#### Важно: Настройка запуска глобального сценария
- После добавления и настройки глобального сценария **не нажимай кнопку "Play"** для его запуска.
- В настройках глобального сценария выключи опцию **"Запускать на старте системы"** (это предотвратит запуск сценария при перезагрузке Sprut.Hub, так как запуск должен происходить через блочный сценарий).
- Глобальный сценарий будет запускаться автоматически через блочный сценарий с триггером "Каждые 5 минут".


#### Создаём или скачиваем готовый (TelegramPresenceBot (блочный)) блочный сценарий для запуска
1. В Sprut.Hub перейди в "Сценарии".
2. Нажми "+" → "Создать блочный сценарий".
3. Назови сценарий, например, "TelegramPresenceBot (настройка)".
4. Добавь блок "Если":
   - Выбери "Таймер" → "Каждые 5 минут", (чтобы уменьшить количество логов).
5. Добавь блок "Тогда":
   - Вставь код:
     if (!GlobalVariables.tg_loop_running) {
         global.tgWatch();
     }
     ```
6. Сохрани и включи сценарий (нажми на кнопку "Play").

#### Настраиваем игнорирование старых сообщений
Сценарий игнорирует сообщения, которые пришли больше чем 10 минут назад, чтобы не обрабатывать устаревшие команды. Но ты можешь это изменить, если хочешь:
- В сценарии найди строку с кодом: `if (messageTime < startTime || (now - messageTime) > 600)`.
- Число `600` — это 10 минут (в секундах). Ты можешь:
  - Увеличить, например, до `1800` (30 минут), если хочешь видеть старые команды.
  - Уменьшить, например, до `300` (5 минут), если хочешь быстрее игнорировать старые сообщения.
- После изменения сохрани сценарий. Это просто, и ты сам решишь, сколько времени ждать!

---

### 4. Настраиваем автоматизацию на iPhone через геопозицию
Теперь настроим iPhone, чтобы он автоматически отправлял команды в Telegram, когда ты приходишь домой или уходишь.

#### Автоматизация "Пришёл домой"
1. Открой приложение "Команды" (Shortcuts) на iPhone.
2. Перейди во вкладку "Автоматизация".
3. Нажми "+" → "Создать автоматизацию для себя".
4. Выбери "Прибытие".
5. Укажи адрес твоего дома (например, "ул. Мира, 5") и радиус (обычно 100 метров достаточно).
6. Нажми "Далее".
7. Нажми "Добавить действие" → найди "Получить содержимое URL".
8. Настрой действие:
   - Метод: GET.
   - URL: Вставь команду для "дома" (например, для Васи):
     ```
     https://api.telegram.org/bot***твой_токен_второго_бота***/sendMessage?chat_id=***твой_ид_канала***&text=***закодированное_имя_пользователя***%20arrived
     ```
     - Замени `***твой_токен_второго_бота***` на токен второго бота (@ProxyFamilyBot) токен должен быть без ***звёздочек***.
     - Замени `***твой_ид_канала***` на ID твоего канала, должен быть без ***звёздочек***.
     - `%D0%92%D0%B0%D1%81%D1%8F` — это закодированное имя "Вася", должно быть без ***звёздочек***. Чтобы закодировать имя:
       - Зайди на сайт [urlencoder.org](https://www.urlencoder.org/).
       - Введи имя (например, "Вася") и нажми "Encode".
       - Скопируй результат.
9. Нажми "Далее" и сохрани. Отключи опцию "Спрашивать перед запуском", чтобы всё работало автоматически.

#### Автоматизация "Ушёл из дома"
1. Повтори шаги, но выбери "Покидание".
2. Укажи тот же адрес (твой дом) и радиус.
3. В действии "Получить содержимое URL" укажи команду для "не дома":
   ```
   https://api.telegram.org/bot***твой_токен_второго_бота***/sendMessage?chat_id=***твой_ид_канала***&text=***закодированное_имя_пользователя***%20left
   ```
4. Сохрани и отключи "Спрашивать перед запуском".

#### Повтори для каждого члена семьи
- Создай автоматизацию для каждого человека (Лена, Галя, Толя), меняя имя в URL (например, `%D0%9B%D0%B5%D0%BD%D0%B0` для Лена).

---

### 5. Добавляем новых пользователей
Если хочешь добавить нового человека (например, Петю):
1. **Создай виртуальное устройство**:
   - В Sprut.Hub добавь устройство для Пети с выключателем и датчиком (см. шаг 2).
   - Настрой "Связь с устройством" между выключателем и датчиком.
2. **Добавь Петю в сценарий**:
   - В сценарии найди `const switchMap = { ... };`.
   - Добавь строку: `'Петя': { accessoryId: 105 }` (замени `105` на его `accessoryId`).
3. **Настрой автоматизацию на iPhone**:
   - Создай две автоматизации ("Прибытие" и "Покидание") с именем Пети:
     ```
     https://api.telegram.org/bot***твой_токен_второго_бота***/sendMessage?chat_id=***твой_ид_канала***&text=***закодированное_имя_пользователя***%20arrived
     ```
     ```
     https://api.telegram.org/bot***твой_токен_второго_бота***/sendMessage?chat_id=***твой_ид_канала***&text=***закодированное_имя_пользователя***%20left
     ```

#### Удаление пользователя
- Найди в `switchMap` строку с именем (например, `'Галя': { accessoryId: 103 }`) и удали её.
- Удали автоматизации для этого человека на iPhone.

---

### 6. Меняем ответы бота
Хочешь, чтобы бот говорил "пришёл" вместо "дома" и "ушёл" вместо "не дома"?
- В сценарии найди строки:
  - `tg().line(name + " дома").send();` — замени на `tg().line(name + " пришёл").send();`
  - `tg().line(name + " не дома").send();` — замени на `tg().line(name + " ушёл").send();`
- Можно придумать свои слова, например, "на месте" и "отсутствует".

---

### 7. Тестируем
1. Запусти автоматизацию на iPhone в ручную.
2. Проверь:
   - Появилось ли сообщение в канале и группе "Семья" (например, "Вася arrived").
   - Написал ли бот в группе "Вася дома" или "Вася не дома".
   - Включился или выключился выключатель в Sprut.Hub.
   - Изменился ли датчик присутствия ("Есть присутствие" или "Нет присутствия").
3. Вручную включи/выключи выключатель в Sprut.Hub и убедись, что датчик меняется (благодаря "Связь с устройством").

---

### 8. Если что-то не работает
- **Бот не пишет в группу**:
  - Проверь, правильно ли ты вставил токен и ID группы в сценарий.
  - Убедись, что боты добавлены в канал и группу как администраторы.
- **Автоматизация на iPhone не срабатывает**:
  - Проверь, включены ли геолокация и уведомления для приложения "Команды".
  - Убедись, что радиус в автоматизации правильный (например, 100 метров).
- **Выключатель и датчик не синхронизируются**:
  - Проверь настройки "Связь с устройством" в Sprut.Hub.
