{
  "scenarioTemplate": {
    "name": "Восход - Закат (по координатам формулой расчёта)",
    "desc": "",
    "active": true,
    "onStart": false,
    "sync": false,
    "data": "// ========== ГЛОБАЛЬНАЯ КОНФИГУРАЦИЯ ==========\nglobal.LAT = 4,17521;    // Широта (необходимо указать для точного расчёта)\nglobal.LNG = 73,5092;    // Долгота (необходимо указать для точного расчёта)\nglobal.TZ = 3;              // Часовой пояс (MSK, UTC+3)\nglobal.ELEVATION = 17;     // Высота над уровнем моря в метрах (добавлено)\n\n// ========== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ==========\nfunction padZero(num) {\n    return num < 10 ? \"0\" + num : num;\n}\n\nfunction formatTime(hours, minutes, seconds) {\n    return padZero(hours) + \":\" + padZero(minutes) + \":\" + padZero(seconds);\n}\n\n// ========== ОСНОВНЫЕ ФУНКЦИИ РАСЧЕТА ==========\nglobal.calculateSunTimes = function() {\n    var date = new Date();\n    var rad = Math.PI / 180;\n    var latRad = global.LAT * rad;\n    var start = new Date(date.getFullYear(), 0, 0);\n    var dayOfYear = Math.floor((date - start) / 86400000);\n    \n    var declination = 23.45 * rad * Math.sin(2 * Math.PI * (284 + dayOfYear) / 365);\n    var B = 2 * Math.PI * (dayOfYear - 81) / 365;\n    var equation = 9.87 * Math.sin(2*B) - 7.53 * Math.cos(B) - 1.5 * Math.sin(B);\n    \n    // Учет высоты над уровнем моря\n    var refraction = Math.acos(6371/(6371+global.ELEVATION/1000)) * (180/Math.PI);\n    var hourAngle = Math.acos(\n        (Math.cos(90.83 * rad) - Math.sin(latRad) * Math.sin(declination)) / \n        (Math.cos(latRad) * Math.cos(declination))\n    ) * 180 / Math.PI - refraction;\n    \n    var sunriseUTC = 12 - hourAngle/15 - equation/60 - (global.LNG/15);\n    var sunsetUTC = 12 + hourAngle/15 - equation/60 - (global.LNG/15);\n    \n    return {\n        sunrise: formatTime(\n            Math.floor(sunriseUTC + global.TZ),\n            Math.floor((sunriseUTC % 1) * 60),\n            0\n        ),\n        sunset: formatTime(\n            Math.floor(sunsetUTC + global.TZ),\n            Math.floor((sunsetUTC % 1) * 60),\n            0\n        ),\n        isDayTime: function() {\n            var now = new Date();\n            var nowMinutes = now.getHours() * 60 + now.getMinutes();\n            var sunriseMinutes = parseInt(this.sunrise.split(':')[0]) * 60 + parseInt(this.sunrise.split(':')[1]);\n            var sunsetMinutes = parseInt(this.sunset.split(':')[0]) * 60 + parseInt(this.sunset.split(':')[1]);\n            return nowMinutes >= sunriseMinutes && nowMinutes < sunsetMinutes;\n        }\n    };\n};\n\n// ========== ТЕСТОВАЯ ФУНКЦИЯ ==========\nglobal.testSunCalc = function() {\n    var times = global.calculateSunTimes();\n    log.info(\n        \"Тест расчета: Восход \" + times.sunrise + \n        \", Закат \" + times.sunset + \n        \", Сейчас \" + (times.isDayTime() ? \"☀️ День\" : \"🌙 Ночь\") +\n        \", Высота: \" + global.ELEVATION + \"м\"\n    );\n};\n/* Проверка корректности расчетов:\nПозволяет вручную проверить, правильно ли рассчитываются время восхода/заката\nПоказывает текущий режим (день/ночь) без управления устройствами\nКоманда для запуска: global.testSunCalc(); <==  Раскоментировать для отладки в логах\n*/\n//global.testSunCalc(); //<==  Раскоментировать для отладки в логах\n\n\n",
    "type": "GLOBAL"
  }
}